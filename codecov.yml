coverage:
  status:
    project:
      default:
        target: 75
        threshold: 1      # allow small fluctuations
    patch:
      default:
        target: 75
        threshold: 1

comment:
  layout: "reach, diff, files"
  behavior: default
  require_changes: false
